package models

import (
	"fmt"
	"testing"
)

const TABLE_PREFIX = "welcome_page_"

func TestGetArticles(t *testing.T) {
	//var articles []Article
	//db.Preloads("Tag").Limit(10).Find(&articles)
	var start *int64
	fmt.Println(start)
}
